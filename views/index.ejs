<!DOCTYPE html>
<html>
  <head>
    <style>
      *{
        margin: 0;
        padding: 0;
        border: 0;
      }
      body {
        padding: 50px;
        font: 14px "Lucida Grande", Helvetica, Arial, sans-serif;
      }

      .download {
        margin-left: 30px;
        line-height: 50px;
        text-decoration:none;
        color: #00B7FF;
      }
      .title{
        line-height: 50px;
      }
      .item{
        padding: 10px;
        height: 50px;
        width: 1000px;
        display: flex;
        justify-content:space-between;
      }
      .item:nth-of-type(odd){
        background: #f0f0f0;
      }
    </style>
  </head>
  <body>
    <h1 style="margin-bottom: 20px">电影天堂 www.dytt8.net</h1>
    <h3 style="margin-bottom: 50px">全站爬虫抓取  时间:2018-08-02</h3>

    <% for (let i=0 ;i<btLink.length;i++){
        %>
    <div class="item">
      <h4 class="title"><%= i+1 %> . <%= btLink[i].title %></h4>
      <div>
        <% if (btLink[i].bt!=undefined&&btLink[i].bt!=''){%> <a class="download" href="<%= btLink[i].bt %>">磁力链接</a> <%} %>
        <% if (btLink[i].ftp!=undefined&&btLink[i].ftp!=''){%> <a class="download" href="<%= btLink[i].ftp %>">FTP链接</a> <%} %>
    </div>
    </div>
    <%
    } %>
  </body>
</html>
